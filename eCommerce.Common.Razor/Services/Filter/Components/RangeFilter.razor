@if(Filter is not null)
{
    <input type="range" min="@Filter.MinValue" max="@Filter.MaxValue" @bind="Filter.SelectedValue" />
}

@code {
    [Parameter]
    public FilterOption? Filter { get; set; }
}
